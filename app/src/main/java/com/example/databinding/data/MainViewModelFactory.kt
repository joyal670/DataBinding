package com.example.databinding.data

import androidx.lifecycle.ViewModel
import androidx.lifecycle.ViewModelProvider

/*
class MainViewModelFactory(private val apiService: ApiService) : ViewModelProvider.Factory {

    override fun <T : ViewModel?> create(modelClass: Class<T>): T {
        if (modelClass.isAssignableFrom(BankViewModel1::class.java)) {
            return BankViewModel1(apiService) as T
        }
        throw IllegalArgumentException("Unknown ViewModel class")
    }
}*/
